## Login

```python
import requests
url = "https://ept.praavahealth.com/Admin/otp/createPhblOtp?token=<token>&upi=<upi>"
payload={}
headers = {}
response = requests.request("GET", url, headers=headers, data=payload)
print(response.text)
```

```javascript
var request = require('request');
var options = {
  'method': 'GET',
  'url': 'https://ept.praavahealth.com/Admin/otp/createPhblOtp?token=<token>&upi=<upi>',
  'headers': {
  }
};
request(options, function (error, response) {
  if (error) throw new Error(error);
  console.log(response.body);
});
```

```shell
curl --location --request GET 'https://ept.praavahealth.com/Admin/otp/createPhblOtp?token=<token>&upi=<upi>'
```

> This is the response we should get

```json
{
    "success": true,
    "result": "OTP Sent",
    "message": "An OTP has been sent to your registered mobile number +88017*****300"
}
```

This endpoint login a patient to the portal. 

### GET HTTP Request

`GET https://ept.praavahealth.com/Admin/otp/createPhblOtp?token=<token>&upi=<upi>`

### Request Information

| Category          | Value                                                                           |
| ---------         | ---------------------------------------------------------------                 |
| HTTP Request      | GET                                                                             |
| URL               | https://ept.praavahealth.com/Admin/otp/createPhblOtp?token=<token>&upi=<upi>      | 

### Headers
No Headers required

### Input Schema of request

| Parameter  | Description                                                     | Required |
| ---------  | --------------------------------------------------------------- | -------- |
| token      | Mandatory token for authorization.                              | true     |
| upi        | UPI of the individual patient.                                  | true     |     

### Output Schema of request

| Filed          | Description                                                     |
| ---------      | --------------------------------------------------------------- |
| success        | status of the request.                                          |
| result         | result of the request.                                      |    
| message        | message of the response                                         |    